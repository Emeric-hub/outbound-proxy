# e2guardianf1.conf — filter group 1 (default group, v5.x syntax)

groupname        = 'default'

# Score threshold above which a page is blocked
naughtynesslimit = 50

# 0=access denied text, 1=HTML template, 2=redirect to URL
reportinglevel   = 1

# 0=no bypass allowed
bypass           = 0
